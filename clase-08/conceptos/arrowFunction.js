// Funcion tradicional:
function suma(a, b) {
  return a + b;
}
// Arrow function (Funcion Flecha)
const suma = (a, b) => {
  return a + b;
};
console(add(2 + 2));
// Las arrow function se escriben como  variables que almacenan funciones anonimas
// su estructura es.
// 1) const nombreFuncion.
// 2) (a, b) parentesis para los parametros.
// 3) => Flechita.
// 4) Llaves cuando escribirás más de una línea de código o
//    Sin llaves cuando solo retornará una línea de código.

// Ejemplo sin llaves,
// ----- Descomente -----
// const add = (a, b) => a + b; // Ahorras rengloes y es facil de leer
// console(add(2+2))
